default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: omarchy_directory_development
  username: omarchy
  password: omarchy
  host: localhost
  port: 5432

test:
  <<: *default
  database: omarchy_directory_test
  username: omarchy
  password: omarchy
  host: localhost
  port: 5432

production:
  <<: *default
  url: <%= ENV.fetch("DATABASE_URL", "") %>

